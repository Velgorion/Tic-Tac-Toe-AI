# 📱 Сборка Android-версии Tic-Tac-Toe-AI с графическим интерфейсом Kivy


## Быстрый старт

1. **Установи зависимости:**
   - Python 3.8–3.11
   - pip install kivy buildozer
   - (Linux: sudo apt install -y build-essential git python3-pip openjdk-17-jdk unzip zlib1g-dev)
2. **Проверь, что в репозитории есть `main.py` и `buildozer.spec`.**
3. **Собери APK:**
   ```
   buildozer -v android debug
   ```
   Файл появится в папке `bin/` — перешли его друзьям для установки на Android!

## Что изменено
- Файл `main.py` — графический интерфейс на Kivy
- Файл `buildozer.spec` — конфиг для сборки APK
- Используется AI из папки `AIs/`, логика игры из `engine.py`

## Советы
- Запускай `main.py` на ПК через `python main.py` для быстрого теста GUI.
- Для публикации в Google Play потребуется Release APK (см. гайды Kivy).
- Если хочется сделать английскую версию или добавить выбор сложности — перепиши в main.py нужные названия и дополни меню. Сейчас по умолчанию работает Minimax-бот (сложно для человека!).


## Полезные ссылки
- Документация: https://kivy.org/doc/stable/
- Buildozer: https://buildozer.readthedocs.io/en/latest/
- Kivy примеры: https://github.com/kivy/kivy/tree/master/examples


---

Если что-то не взлетело — смотри логи Buildozer и issues в темах Kivy/Buildozer.